{
  "name": "equinext-basket",
  "version": "1.0.0",
  "private": true,
  "description": "Equinext valuation-momentum basket — Flask web app over local SQLite databases.",
  "scripts": {
    "dev": "cd equinext_baskets-main && python webapp/app.py",
    "start": "cd equinext_baskets-main && python webapp/app.py"
  }
}
